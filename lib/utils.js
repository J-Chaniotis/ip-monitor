'use strict';

var revalidator = require('revalidator');

var validateConfig = function (config) {

};

var mergeConfig = function (external, default) {

};

module.exports = {
    validateConfig: validateConfig,
    mergeConfig: mergeConfig
};